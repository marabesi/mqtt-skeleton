{
  "name": "mqtt-skeleton",
  "version": "0.0.1",
  "description": "Easy way to get start using MQTT (nodejs)",
  "main": "index.js",
  "scripts": {
    "nodemon": "concurrently --kill-others 'nodemon ./index.js' 'http-server ./hivemq-mqtt-web-client'",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node ./index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/marabesi/mqtt-skeleton"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^3.5.1",
    "dotenv": "^4.0.0",
    "http-server": "^0.11.1",
    "mqtt": "^2.15.1"
  },
  "devDependencies": {
    "nodemon": "^1.14.11"
  }
}
